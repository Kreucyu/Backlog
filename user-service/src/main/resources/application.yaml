spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/usuario
    username: postgres
    password: 123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

api:
    security:
      token:
        secret: ${JWT_SECRET:my-secret-key}
